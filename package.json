{
  "scripts": {
    "dev": "snowpack dev",
    "build": "snowpack build",
    "build-and-delete": "snowpack build && find build -name '*.elm' -type f -delete",
    "test": "elm-test",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "devDependencies": {
    "@snowpack/plugin-sass": "^1.4.0",
    "elm-test": "^0.19.1-revision6",
    "fast-glob": "^3.2.5",
    "prettier": "~2.1.2",
    "snowpack": "~3.0.13",
    "snowpack-plugin-elm": "^0.4.2"
  }
}
